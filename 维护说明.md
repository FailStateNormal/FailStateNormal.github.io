# 网站维护说明

写给自己看的日常操作指南。第一次维护时从头看一遍即可。

## 1. 三份"网站"的关系

| 在哪 | 是什么 | 谁能看到 |
| --- | --- | --- |
| 你的电脑（这个文件夹） | 改文件的地方 | 只有你 |
| GitHub 仓库 | 云端备份 | 存在网上 |
| GitHub Pages | 真正的网站 | 全世界 |

流程永远是一个方向：

```text
你的电脑  ──推送──▶  GitHub  ──自动构建──▶  线上网站
```

**关键：光在电脑上改文件，网站不会变，必须推送上去才生效。**

## 2. 日常循环（最常用）

1. 改文件（多数时候是 `assets/data/site-content.js`）
2. 双击对应的 `.html` 文件，用浏览器预览效果
3. **双击 `更新网站.bat`**，输入一句说明，回车
4. 等 1~2 分钟，刷新 https://FailStateNormal.github.io

如果不想用脚本，也可以在这个文件夹打开 PowerShell 手动执行：

```powershell
git add -A
git commit -m "说明你改了什么"
git push origin main
```

## 3. 想改什么，开哪个文件

| 想改什么 | 改哪个文件 |
| --- | --- |
| 音乐 / 历史 / 物理 / 书影音 内容 | `assets/data/site-content.js` |
| ACM 模板内容 | `assets/data/acm-data.js` |
| 首页文字、栏目入口、联系方式 | `index.html` |
| 颜色、字体、布局、动画 | `assets/css/style.css` |
| 私人待办笔记 | `docs/what-to-do.txt` |

## 4. 怎么改 `site-content.js`

这个文件像一张填空表，只改引号里的字即可：

```js
title: '方大同',
summary: '这里写简介……',            // ← 改这里
bullets: ['风格：……', '代表作：……']   // ← 或这里
```

注意：保持引号 `'...'`、逗号 `,`、大括号 `{ }` 不动，就不会改坏结构。

## 5. 常见问题

- **改了样式或脚本，但网站没更新？**
  每个 HTML 文件里 CSS/JS 链接后面都有 `?v=20260607-1`。用编辑器的「全局查找替换」把所有 `20260607-1` 换成新的一版（如 `20260607-2`），再推送，可强制浏览器刷新缓存。注意要替换**全部页面的全部位置**，别只改一处。
- **推送被拒 / 报错？**
  先运行 `git pull origin main` 把远程改动拉下来，再推送。
- **不确定改了哪些文件？**
  运行 `git status` 查看。

## 6. 注意

- 勤推送、小步走：一个主题一次提交，出问题好排查。
- 只要推送了，GitHub 就是你的备份，电脑坏了也不怕。
- 不要手动改 `.git` 文件夹，那是 git 的内部账本。
