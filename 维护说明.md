# 网站维护说明

写给自己看的日常操作指南。第一次维护时从头看一遍即可。

## 1. 三份"网站"的关系

| 在哪 | 是什么 | 谁能看到 |
| --- | --- | --- |
| 你的电脑（这个文件夹） | 改文件的地方 | 只有你 |
| GitHub 仓库 | 云端备份 | 存在网上 |
| GitHub Pages | 真正的网站 | 全世界 |

流程永远是一个方向：

```text
你的电脑  ──推送──▶  GitHub  ──自动构建──▶  线上网站
```

**关键：光在电脑上改文件，网站不会变，必须推送上去才生效。**

## 2. 日常循环（最常用）

1. 改文件（多数时候是 `assets/data/site-content.js`）
2. 双击对应的 `.html` 文件，用浏览器预览效果
3. 用下面的 PowerShell 命令提交并推送
4. 等 1~2 分钟，刷新 https://FailStateNormal.github.io

在这个文件夹打开 PowerShell 执行：

```powershell
git add -A
git commit -m "说明你改了什么"
git push origin main
```

## 3. 想改什么，开哪个文件

| 想改什么 | 改哪个文件 |
| --- | --- |
| 音乐 / 历史 / 物理 / 书影音 内容 | `assets/data/site-content.js` |
| ACM 模板内容 | `assets/data/acm-data.js` |
| 首页文字、栏目入口、联系方式 | `index.html` |
| 颜色、字体、布局、动画 | `assets/css/style.css` |
| 悬浮播放器的歌单 | `assets/js/player.js` 顶部的 `TRACKS` 列表 |
| 幻灯片照片 | `assets/img/slides/` 各文件夹 + `assets/js/script.js` 里的 `SLIDE_SETS` |
| 私人待办笔记 | `docs/what-to-do.txt` |

### 加一首歌

1. 把 FLAC/无损文件放进 `material/music/`（此文件夹不进仓库，只是本地素材库）
2. 用 ffmpeg 转成 mp3 放进 `assets/media/music/`（FLAC 太大，GitHub 单文件上限 100MB）：
   ```powershell
   ffmpeg -i "material\music\歌名.flac" -vn -codec:a libmp3lame -q:a 4 "assets\media\music\11-xxx.mp3"
   ```
3. 在 `assets/js/player.js` 顶部的 `TRACKS` 里照样子加一行文件名和标题

### 加几张照片

1. 图片放进 `assets/img/slides/对应文件夹/`，文件名按 `01.jpg`、`02.jpg` 顺延
2. 在 `assets/js/script.js` 的 `SLIDE_SETS` 里把对应数量改大（如 `setOf('music', 17)`）

## 4. 怎么改 `site-content.js`

这个文件像一张填空表，只改引号里的字即可：

```js
title: '方大同',
summary: '这里写简介……',            // ← 改这里
bullets: ['风格：……', '代表作：……']   // ← 或这里
```

注意：保持引号 `'...'`、逗号 `,`、大括号 `{ }` 不动，就不会改坏结构。

## 5. 常见问题

- **改了样式或脚本，但网站没更新？**
  每个 HTML 文件里 CSS/JS 链接后面都有 `?v=20260707`。用编辑器的「全局查找替换」把所有 `20260707` 换成新的一版（如当天日期），再推送，可强制浏览器刷新缓存。注意要替换**全部页面的全部位置**，别只改一处。
- **打开网页音乐没自动响？**
  正常。浏览器规定页面必须先被点击一次才允许出声，所以右下角会先弹提示「点一下唱片」，点一下就开始播了。
- **推送被拒 / 报错？**
  先运行 `git pull origin main` 把远程改动拉下来，再推送。
- **不确定改了哪些文件？**
  运行 `git status` 查看。

## 6. 注意

- 勤推送、小步走：一个主题一次提交，出问题好排查。
- 只要推送了，GitHub 就是你的备份，电脑坏了也不怕。
- 不要手动改 `.git` 文件夹，那是 git 的内部账本。
